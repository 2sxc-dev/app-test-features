@inherits Custom.Hybrid.Razor14
@using ToSic.Sxc.Code;
@using ToSic.Eav.DataSources;

<h1>App DataSources</h1>

@{
  var expected = 6;
  var ds = Kit.Data.GetSource(name: "MagicDataSource", parameters: new [] { "AmountOfItems=" + expected, "FavoriteNumber=69" });
  ds = Kit.Data.GetSource(name: "MagicDataSource", parameters: new { AmountOfItems = expected, FavoriteNumber = -42 });
}

@Html.Partial("./_Helpers/ShowDataInSource.cshtml", new { List = ds.List, Expected = expected })