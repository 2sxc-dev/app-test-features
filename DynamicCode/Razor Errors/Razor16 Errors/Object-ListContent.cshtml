@inherits Custom.Hybrid.RazorPro
@{
  // This should throw an error and provide guidance in the html and toolbar

  // Reason is that the `ListContent` object was removed and should be replaced with Header.
  var x = ListContent;
}