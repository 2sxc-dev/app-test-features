@inherits Custom.Hybrid.RazorTyped

<h1>Mail Service</h1>

<p>
  Tests for <code>Kit.Mail</code> / <code>IMailService</code>.
</p>

<p>
  The create tests do not send email, so they can run without SMTP configuration.
</p>
