@inherits Custom.Dnn.Razor12
@{
  // This should throw an error and provide guidance in the html and toolbar

  // Reason is that the `Presentation` object was removed and should be replaced with Content.Presentation.
  var x = Presentation;
}