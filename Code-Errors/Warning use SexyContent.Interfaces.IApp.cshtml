@inherits Custom.Hybrid.Razor14
@using ToSic.Sxc.Services;

<h1>Warning for SexyContent.Interfaces.IApp</h1>
<p>
  This should show a warning because we're activating an interface that's obsolete and will be removed. 
</p>
@{
  ToSic.SexyContent.Interfaces.IApp app = App;

  var t = app.Thumbnail;
  var p = app.Path;
  var pp = app.PhysicalPath;
  var s = app.Settings;
  var r = app.Resources;
  var c = app.Configuration;
}