@inherits Custom.Hybrid.Razor16
@{
  // This should throw an error and provide guidance in the html and toolbar

  // Reason is that the `ListPresentation` object was removed and should be replaced with Header.Presentation.
  var x = ListPresentation;
}