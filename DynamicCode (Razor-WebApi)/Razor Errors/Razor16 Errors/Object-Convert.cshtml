@inherits Custom.Hybrid.Razor16
@{
  // This should throw an error and provide guidance in the html and toolbar

  // Reason is that the `Convert` object was removed and should be replaced with Kit.Convert.
  var x = Convert.ToInt("14");
}